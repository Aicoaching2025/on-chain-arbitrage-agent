# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# DuckDB file is large (~38MB) and regenerated by scripts/run_backtest.py
data/*.db
data/_test.db
data/_st.log
data/_st.err

# NOTE: data/backtest_results.json and data/walk_forward_results.json ARE
# committed on purpose so the deployed dashboard (Streamlit Cloud) loads
# instantly without a build step. The app also self-bootstraps if they're
# missing. Delete them from git if you'd rather always regenerate.

# OS / editor
.DS_Store
.streamlit/secrets.toml
